# Kotlin Health Monitor Documentation

The canonical, verified README lives at the repository root: [`README.md`](../README.md).

It describes how to run the Kotlin JVM service, configure the environment, and execute verification.
